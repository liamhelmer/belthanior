# Channel Memory: #mxdx
**Guild:** 1473159530316566551
**Channel ID:** 1485151340614254673
**Purpose:** Ongoing mxdx-fabric development — primary active project channel

---

## 2026-03-21 — Session 1: Channel created

- New dedicated #mxdx channel to replace old channel (1476681685566230810)
- mxdx-fabric fully built and deployed (5 phases, 14 tests passing)
- coordinator + worker running as services on belthanior
- `fabric` CLI at `/home/openclaw/.local/bin/fabric`
- Coordinator room: `!qfGJpxN7byKDeGfbkA:ca1-beta.mxdx.dev`
- Config: `~/.config/mxdx-fabric/config.toml`

---

## 2026-03-22–23 — openclaw-fabric-plugin + SDK fix

- openclaw-fabric-plugin built and installed at `~/.openclaw/extensions/openclaw-fabric-plugin`
- **SDK was wrong first time:** used `execute` key, spaces in command names, wrong export
- **Correct SDK:** `export default function register(api)`, `api.registerCommand({ name: "kebab-case", handler: async (ctx) => {} })`
- Commands registered: `/fabric-submit`, `/fabric-history`, `/fabric-watch`, `/fabric-logs`
- Plugin registered and loading correctly
- fabric history snapshot: tasks ec79b93f, 1fe1b074, 7d76bdaa, a804eec1, 2a824dba (all completed)
- hookwise brainstorm task (2a824dba) completed via fabric: 924-line doc, 7 plugin primitives

---

## 2026-03-24 — Fabric plugin claude output + e2e tests

- fabric-plugin rewritten to support claude `--output-format stream-json` (NDJSON parsing)
- `parseNdjson()` + `decodeClaudeStreamJson()` added to plugin
- fab-plugin-claude-output session: build succeeded, committed
- fabric-e2e-tests session spawned: add tests for ProcessWorker + plugin NDJSON parsing
- Process_worker refactor also done in this session

---

## 2026-03-24 — TLS fix, disk cleanup, vLLM config

- mxdx TLS fix applied (details in daily note)
- Disk cleanup: GLM-4.7-Flash (59GB), Qwen3-30B-A3B (57GB), Qwen2.5-7B-Instruct (15GB) deleted
- `model-list` and `model-prune` subcommands added to `/home/models/vllm-server.py`
- Sudoers expanded: podman image prune, rocm-maintenance.sh now NOPASSWD
- `extra_args = ["--enforce-eager"]` added to config.toml (TOML scoping fix: must be top-level, before first [section])

---

## 2026-03-24/25 — Fabric chain validation + worker capabilities

- Worker was running since Mar 22 without `claude,jcode` capabilities
- Restarted worker with `--capabilities rust,linux,bash,claude,jcode`
- fabric e2e validated: task post → worker claims → executes → result event in Matrix room
- **"/fabric-submit from bot doesn't trigger plugin handler"** — plugin commands must be typed by user in Discord; bot sending `/fabric-submit` as a message routes to AI agent, not plugin
- fabric-config-mapping task spawned (WITHOUT APPROVAL — noted and corrected): add full CLI→config file mapping for worker/coordinator flags
- "No coding without authorization" rule reinforced in AGENTS.md

---

## 2026-03-25 — Memory audit + AGENTS/MEMORY updates

- Memory audit report generated and reviewed by Liam
- Full implementation of all audit recommendations approved and applied:
  - AGENTS.md: stronger no-coding rule with self-checklist, sub-agent guardrails, mandatory end-of-session flush, new channel proactive maintenance
  - MEMORY.md: recurring violation note, gateway hazard, voice escalation pattern, sub-agent discipline, all missing facts added
  - 6 new channel memory files created
  - 2 stale channel memory files updated
- reserveTokens changed to 700000 in openclaw.json

---

## Infrastructure Facts
- Coordinator room: `!qfGJpxN7byKDeGfbkA:ca1-beta.mxdx.dev`
- Config: `~/.config/mxdx-fabric/config.toml`
- Worker capabilities: `rust,linux,bash,claude,jcode`
- Worker PID: 1508033 (started 2026-03-24 23:51 PDT)
- Repo: ~/workspace/mxdx (Epiphytic org on GitHub)
