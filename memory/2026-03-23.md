# Memory — 2026-03-23

## openclaw-fabric-plugin
- Fixed `source: "path"` (not `"local"`) for local plugins in openclaw.json installs entry
- Fixed PluginContext interface: added `pluginConfig?`, `getConfig?()` alongside `config?`
- Plugin now loads cleanly on gateway restart — confirmed `[fabric] Creating outbox room: openclaw-fabric-8557122562c3` in gateway status output
- Plugin registers `/fabric watch` slash command for callback registration
- Binary rebuild needed both `mxdx-fabric` AND `mxdx-fabric-cli` crates — the CLI lives in separate crate

## mxdx fabric CLI
- Binary is `target/release/fabric` (not `mxdx-fabric`)
- `--payload-json` flag lives in `mxdx-fabric-cli` crate (commit 3386157), not `mxdx-fabric`
- `fabric watch --task-uuid <uuid> --room <room>` — only these args supported, no `--notify`
- `/fabric watch` slash command in Discord is the correct way to register callbacks from chat

## hookwise
- Repo: https://github.com/Epiphytic/hookwise (Epiphytic org, Liam's project)
- Rust binary, Claude Code plugin, AI-assisted permission gating
- 7-stage cascade: Sanitize → Path Policy → Cache → Token Jaccard → Embedding HNSW → LLM → Human
- Three decision states: allow (converging), deny (converging), ask (non-converging — always escalates)
- Design doc: docs/captain-hook-design.md
- Cloned locally to ~/workspace/hookwise

## Active fabric task (2026-03-23 ~08:15 PDT)
- Task UUID: ec79b93f-a5da-49ed-97fa-89f604f7fbe4
- Prompt: brainstorm hookwise-as-jcode-plugin (Parts A/B/C: jcode plugin model, hookwise integration, infra gaps)
- Output target: ~/workspace/hookwise/docs/jcode-plugin-brainstorm.md
- Callback registered via `/fabric watch` to Discord #mxdx channel 1485151340614254673
- Status: running (jcode working on it)

## hookwise brainstorm (wrong one — mxdx context)
- First brainstorm task went to mxdx repo by mistake (assumed webhook interceptor context)
- File: mxdx/docs/brainstorms/2026-03-23-hookwise-webhook-interception.md (pushed to GitHub)
- Correct task is jcode plugin integration, not a new webhook tool

## jcode brainstorm skill
- Located at ~/.jcode/skills/brainstorm/
- Use for structured design exploration before implementation

## Callback routing status
- Plugin is live and initialized on Matrix
- End-to-end callback test: task ec79b93f is the first real test of the full flow
- Sync wait loop in fabric CLI panics after task completes (known issue, non-fatal)
