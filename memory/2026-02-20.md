# 2026-02-20 - First day online

## Bootstrap
- Came online, introduced myself, got named Belthanior (Bel) by Liam
- Hostname matches: belthanior

## About Liam
- Platform engineer, dancer, farmer on Vancouver Island
- Father of a 4-year-old daughter
- Builds a lot with AI

## Liam's Projects
- **Active:** App for automatically building WASM-based utilities for AI assistants
- **Back-burnered:** Social media app on top of Matrix

## Preferences
- Signal preferred messaging channel
- Discord working as of 2026-02-20: guild 1473159530316566551, channel 1473159531365269527 (GIRT workspace server)
- Wants general-purpose help: technical, life, sounding board

## Coding Standards
- ~/CLAUDE.md is Liam's coding standards (copied to workspace/CLAUDE.md)
- Key: plan before code, HITL gates, plans in /docs/plans, ADRs in /docs/adrs
- MANIFEST.md tracks all agents/modules/facades — update it on every addition
- Facade pattern for all external APIs (LlmClient is the template)
- Conventional commits, atomic, no bundled diffs
- Circuit breaker: 3 failures → halt and escalate
- Troubleshooter subagent for diagnostics (read-only, no fixing)

## TODO
- Add AnthropicLlmClient to girt-pipeline (behind existing `anthropic` feature flag)
  - New file: crates/girt-pipeline/src/llm/anthropic.rs
  - Implement LlmClient trait, call https://api.anthropic.com/v1/messages
  - Wire into orchestrator via girt.toml config
  - Could also add GlmLlmClient for the local model as a parallel option
- Liam has a sub-agent running E2E tests on girt — check in the morning

## GIRT Session — 2026-02-20 evening

### Major work done today (in order):

**Architecture decision: embed girt-runtime instead of Wassette subprocess**
- Wassette (Microsoft, MIT) is "not production ready" — we're porting its execution core
- ADR-010 written, ADR-001 superseded
- Technical design doc updated throughout (~25 Wassette references replaced)
- ADR-007 updated (pipeline lead workflow)

**AnthropicLlmClient (girt-pipeline)**
- Implemented in `crates/girt-pipeline/src/llm.rs`
- Credential resolution: ANTHROPIC_API_KEY → OpenClaw auth-profiles.json → api_key in girt.toml
- `openclaw_anthropic_token()` reads `~/.openclaw/agents/main/agent/auth-profiles.json`
- Token is `sk-ant-o...` (setup-token / OAuth), used as regular API key header
- girt.toml now defaults to `provider = "anthropic"` / `claude-sonnet-4-5`

**girt-runtime crate (new)**
- `RuntimeContext`: Wasmtime engine + Linker, component model + async + WASI p2 + HTTP
- `WasiState`: per-invocation sandbox (deny-default: no fs, no env vars)
- `ComponentStorage`: disk cache, `.cwasm` precompile, `.metadata.json` sidecar
- `LifecycleManager`: load_component / call_tool / list_tools / unload / load_persisted
- WIT world: `girt-tool` — `run(input: string) -> result<string, string>`
- Ported from Wassette MIT (credited in NOTICE/headers)

**girt-proxy wired up**
- GirtProxy now holds `Arc<LifecycleManager>` (no Wassette subprocess)
- list_tools: live from runtime
- call_tool: real Wasmtime invocation
- trigger_build: full chain — Orchestrator → WasmCompiler → publish_with_wasm → load_component → tools/list_changed

**Similarity layer**
- Was fully implemented but orphaned — wired into Creation Gate cascade (between cli_check and llm_eval)
- Added `Similarity` variant to `DecisionLayer` enum

**Real LLM in decision gates**
- `GateLlmEvaluator` in `girt-proxy/src/evaluator.rs` — bridges pipeline LlmClient to girt-core LlmEvaluator trait
- Separate system prompts for Creation Gate and Execution Gate
- Both gates share the same `Arc<dyn LlmClient>` (one credential, one client)
- `DecisionEngine::with_real_llm()` constructor added

**Smoke test**
- celsius_to_fahrenheit: compile → load → list_tools → call_tool → error handling → persist → reload
- All passing in ~2.9s: `cargo test -p girt-runtime --test integration_test -- --include-ignored`

**Install / MCP setup**
- `cargo install --path crates/girt-proxy` → `~/.cargo/bin/girt`
- Config search order: --config flag → ./girt.toml → ~/.config/girt/girt.toml
- `.mcp.json` in repo root: `{"command": "girt"}` — no hardcoded paths
- `scripts/install.sh` handles full install
- Verified: MCP initialize → list_tools → `request_capability` tool shows up

### Current GIRT state
- Pipeline: Orchestrator (Claude) → WasmCompiler → girt-runtime → agent
- All gap analysis items from Claude's review addressed except: queue watching (ADR-008, not urgent), OCI pull/push
- `cargo-component` and `wasm32-wasip1` installed on belthanior
- Ready for real `request_capability` test from Claude Code

### Next session priorities
1. Real end-to-end test: open girt/ in Claude Code, request a tool
2. MANIFEST.md needs updating (girt-runtime added, proxy changed significantly)
3. Queue watcher (ADR-008): inotify via `notify` crate
4. LLM eval layer for the pipeline agents could use model-specific prompting tuning
