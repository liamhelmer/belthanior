# 2026-02-25

## Voice Bot Fixes (morning session via voice escalation)
- Liam chatting via Chip voice bot, escalating questions to me
- Identified issues:
  1. **Word corrections needed**: Whisper transcribes "Bel" as "Bell"/"Bill" frequently
  2. **Audio clipping**: First word/syllable sometimes cut off at start of speech
  3. **Escalation TTS not playing**: Liam sees my text responses in channel but doesn't hear them spoken — needs investigation
  4. **My responses weren't posting to Discord channel** — fixed by using message tool explicitly
- Spawned coding sub-agent, implemented both fixes in ~3 min:
  - Post-STT word correction dictionary (TOML config, regex word-boundary matching)
  - Audio pre-buffer (300ms rolling deque per user, prepended on speech start)
- PR #4: https://github.com/Epiphytic/openclaw-voice/pull/4
- Production corrections config: `/home/models/corrections.toml`
- Branch: feat/corrections-and-prebuffer

## Voice Bot Fixes (afternoon session ~12:30-1:00 PM)

### Three-fix commit: barge-in + transcript timing + escalation bugs
1. **Barge-in support**: Playback worker now checks `tts_cancel` before/during playback, calls `vc.stop()` immediately on new speech, drains queued audio chunks
2. **Transcript timing**: Moved `_post_to_channel` from LLM worker to playback worker — posts AFTER audio plays, not before. Playback queue now carries `(audio_bytes, channel_post_text)` tuples
3. **Escalation tool bug**: `escalate_to_bel` now returns immediately from `call_llm_with_tools` instead of looping for another LLM round that generated chatty hedging text
4. **Curly quote fallback bug**: LLM used Unicode `'` (U+2019) in "I'll check" — fallback detector only matched ASCII `'`. Now normalizes curly quotes before matching
5. **`speech_end_delay_ms` made configurable**: Via TOML config, wired through CLI → VoiceBot → VoiceSink. Default 1000ms.

### Root cause of msg 1476319368072659177
Chip said "I'll check with Bel about making the end speech detection delay configurable in the plugin settings" — the LLM's own chatty response posted as a Chip message instead of triggering escalation. Two bugs:
- `escalate_to_bel` tool handler didn't return; looped back and LLM generated hedging text
- Curly apostrophe `'` didn't match straight `'` in fallback triggers

### Config note
- `vad_silence_ms` is essentially unused in v2 (debounce timer replaced VAD end-detection)
- `speech_end_delay_ms` is the one that matters — controls when Chip decides you're done talking
- Liam asked about consolidating these two settings

### Commits
- `088c3ee` on feat/speech-delay-and-reconnect (barge-in + transcript timing)
- `aa0d9ce` on feat/v2-conversation-log (escalation + curly quote + configurable delay)

## Voice Bot V2 Features (afternoon ~2-4 PM)

### Single Entity UX + Cast of Characters + Text-to-Voice Bridge
Major session implementing seamless single-entity experience and text channel integration:

1. **Text channel context injection** (`633b3d2`): LLM worker fetches last N messages from linked text channel, injects as context block. `channel_context_messages = 10` in TOML.
2. **Text-to-voice bridge** (`633b3d2`): `on_message` handler synthesizes text channel messages to TTS for voice participants. Messages >250 words summarized via LLM. `tts_read_channel = true` configurable.
3. **Single entity UX** (`39518d1`): System prompt rewritten — "appear as SINGLE assistant", never mention Bel/tools/backends. Interim escalation text: just "One moment."
4. **Cast of characters** (`447cfd2`): `_build_cast()` fetches all guild members, assigns roles (self/agent/bot/user). O(1) lookup. `on_message` rewritten with role-based routing. TTS bridge skips agent bot + voice members.
5. **Async display name resolution + PID file** (`6d8b243`): Cache + API fallback for names. PID file kills previous instance on startup.
6. **NO_REPLY filter** (`e37e8c7`): Escalation responses of "NO"/"NO_REPLY"/"N/A" treated as failed — Chip says "Sorry, I couldn't get an answer."
7. **Privileged intents**: Message Content + Server Members both enabled on bel-audio app in Discord Developer Portal.

### Plugin Packaging Plan
- Detailed plan written: `openclaw-voice/docs/plans/plugin-packaging.md`
- Scope: `@epiphytic/openclaw-discord-voice` (community plugin)
- Architecture: TS plugin spawns/manages Python process, local HTTP/JSON-RPC communication
- Replaces `gateway_client.py` (~344 lines WebSocket+Ed25519) with ~15-line HTTP POST escalation client
- 5 phases: HTTP Control → TS Scaffold → Escalation Bridge → Agent Tools → Cleanup
- One Python process per guild for crash isolation
- SSE push for streaming escalation keepalives
- `escalate_to_bel` tool to be renamed to something generic

### Key commits on feat/v2-conversation-log
`daaf424`, `633b3d2`, `c8868af`, `39518d1`, `447cfd2`, `6d8b243`, `e37e8c7`

## TODO
- [ ] Consider consolidating vad_silence_ms and speech_end_delay_ms
- [ ] Live test full V2 + text-to-voice bridge + escalation + cast end-to-end
- [ ] Merge PRs #4 and #5
- [ ] Begin plugin packaging Phase 1: HTTP control layer + escalation_client.py
- [ ] Rename `escalate_to_bel` tool to generic name
- [ ] Fix CLI default override bug (click option defaults to None for config fallthrough)
