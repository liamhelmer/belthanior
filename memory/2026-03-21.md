# 2026-03-21 — Session Notes

## mxdx-fabric: Full Build Sprint (Phases 3–5)

### Phase 3 — Failure Policies
- Committed `8e223d8`
- Added Escalate/Respawn/RespawnWithContext/Abandon failure policy enforcement in coordinator
- 12/12 tests passing

### Phase 4 — P2P Unix Socket Streaming
- Committed `e657752`
- High-volume task output streamed directly over Unix socket (bypasses Matrix for data)
- `test_p2p_stream_unix_socket` passing

### Phase 5 — `fabric` CLI Binary
- Commits: `fec176b` (CLI), `db4e844` (skills), `5fdb9e6` (daemon subcommands)
- `crates/mxdx-fabric-cli/` created — `fabric` binary with subcommands:
  - `post` — post task, wait for result (reqwest, Matrix REST API)
  - `status` — check task status
  - `watch` — tail heartbeats
  - `coordinator` — long-running coordinator daemon
  - `worker` — long-running worker daemon
- `MatrixClient::connect_with_token()` added to mxdx-matrix (matrix-sdk 0.16 restore_session)
- Binary installed: `~/.local/bin/fabric`
- Startup script: `~/.local/bin/fabric-start.sh`
- Skills written: `~/.jcode/skills/mxdx-fabric/SKILL.md` and `~/openclaw/skills/mxdx-fabric/SKILL.md`
- 14/14 tests passing (including `test_fabric_cli_post`)

### Real Infrastructure Setup
- **API base:** https://mxdx-hosting.liam-helmer-428.workers.dev
- **Registration key:** b46d8fa193ba3afd7fdc71a8d8d48d21
- **Homeserver in use:** ca1-beta.mxdx.dev (Montreal)
- **Coordinator room:** `!qfGJpxN7byKDeGfbkA:ca1-beta.mxdx.dev`
- Accounts provisioned (credentials at `~/.config/mxdx-fabric/credentials.toml`):
  - `@bel-coordinator:ca1-beta.mxdx.dev` — coordinator bot
  - `@bel-worker:ca1-beta.mxdx.dev` — jcode worker
  - `@bel-bel-sender:ca1-beta.mxdx.dev` — Bel/OpenClaw sender
- Config at `~/.config/mxdx-fabric/config.toml` (sender token default)
- Power levels fixed: `state_default: 0`, worker/sender/coordinator at 50/50/100

### End-to-End Status
- Fabric loop working: `fabric post` → coordinator room → worker claims → jcode runs → result back
- **Known issue:** `JcodeWorker` needs a `workdir` pointing to a git repo (currently exits code 2)
  - jcode requires a git repo context; bare prompts without a workdir fail immediately
  - Fix: make `JcodeWorker` accept a configurable `workdir` param; default to mxdx repo or cwd

### Pending
- Fix JcodeWorker workdir (small patch needed)
- `fabric-start.sh` starts both daemons but worker crashes on capability advertise if power levels wrong (now fixed)
- Persistent daemon management (systemd units) — deferred, startup script is fine for now
- The WARN "device might have been deleted" noise — cosmetic, matrix-sdk crypto store is fresh each run; acceptable until we add persistent store

## Key Lesson
- jcode integration tests have been timing out due to running long tasks inline
- fabric solves this: jcode delegates via `fabric post`, the actual work happens in a worker with no timeout
- This was the core motivation for building the fabric in the first place
