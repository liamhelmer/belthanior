# Channel Memory: #mxdx (old channel, now archived)
**Guild:** 1473159530316566551
**Channel ID:** 1476681685566230810
**Purpose:** mxdx-fabric development work — building the distributed task coordination system

---

## 2026-03-21 — Session 1: mxdx-fabric full build sprint

**Topics:**
- Built mxdx-fabric from scratch across 5 phases using jcode sub-agents
- Deployed to ca1-beta.mxdx.dev (Liam's mxdx hosting infrastructure)
- discord-pipe NDJSON decoding added

**Decisions:**
- All Rust (no Python), TypeScript for OpenClaw integrations only
- Coordinator + worker run as systemd user services (`mxdx-fabric-coordinator.service` + `mxdx-fabric-worker.service`)
- jcode workers should have `cwd` set via TaskEvent payload (fixes exit code 2)
- `no_update` and `no_selfdev` default true in worker (workers shouldn't self-modify)
- Epiphytic/jcode fork created 2026-03-21 for future custom patches

**What was built:**
- Phase 1: Event types, crate scaffold, CapabilityIndex, coordinator routing loop, worker claim race, E2E tests (8 tests)
- Phase 2: invite_user, JcodeWorker adapter, SenderClient, integration tests (full sender→worker pipeline)
- Phase 3: Failure policies (Escalate/Respawn/RespawnWithContext/Abandon) + coordinator heartbeat enforcement
- Phase 4: P2P Unix socket streaming for high-volume tasks
- Phase 5: `fabric` CLI binary (post/status/watch), jcode skill, OpenClaw skill
- All 14 tests passing at completion

**Infrastructure:**
- Matrix homeserver: ca1-beta.mxdx.dev (Montreal), federated with ca2-beta.mxdx.dev (West Coast)
- Accounts: `@bel-coordinator:ca1-beta.mxdx.dev`, `@bel-worker:ca1-beta.mxdx.dev`, `@bel-bel-sender:ca1-beta.mxdx.dev`
- Coordinator room: `!qfGJpxN7byKDeGfbkA:ca1-beta.mxdx.dev`
- Config: `~/.config/mxdx-fabric/config.toml`
- CLI: `/home/openclaw/.local/bin/fabric`
- Worker binary: `/home/openclaw/.local/bin/fabric` (worker subcommand)
- Logs: `~/.local/share/mxdx-fabric/logs/`
- Tokens in config (bel-coordinator, bel-worker, bel-bel-sender)

**jcode worker options mapped:**
- `cwd` → `-C` (required for proper operation — must be absolute git repo path)
- `provider`, `model`, `resume_session`, `quiet`, `no_update`, `no_selfdev`, `trace`, `output_format`
- Input sanitized: cwd (no `..`), model (regex allowlist), provider (enum), resume_session (alphanumeric+hyphens)

**Open items / carried to #mxdx:**
- jcode OAuth token expires ~6h after last auth — headless refresh not yet solved
- Output decoder for `fabric post` CLI (raw NDJSON currently)
- jcode as both worker (receiving tasks) AND sender (issuing subtasks to other workers) — key architectural point
- jcode worker based on mxdx-launcher PTY/tmux infra (not just subprocess) — future improvement

**Context shift:**
- Channel renamed/replaced by #mxdx (1485151340614254673) for continued work
